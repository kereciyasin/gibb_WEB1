// 1. Erstelle eine neue Datei mit dem Namen datatypes.js

// 2. Deklariere die folgenden Variablen mit den angegebenen Werten:

//    Eine Boolean-Variable mit dem Namen isTrue und dem Wert true
//    Eine Boolean-Variable mit dem Namen isFalse und dem Wert false
let isTrue = true;

//    Eine Zahl mit dem Namen num und dem Wert 42
let num = 42;
//    Einen String mit dem Namen str und dem Wert "Hallo Welt"
let str = "Hallo Welt";

//    Eine Variable mit dem Namen undef ohne Wert (undefined)
let undef;
//    Eine Variable mit dem Namen nully mit dem Wert null
let nully = null;
// 3. Verwende den typeof-Operator, um den Typ jeder Variablen zu überprüfen und gib das Ergebnis in der Konsole aus.
console.log(typeof isTrue);
console.log(typeof isFalse);
console.log(typeof num);
console.log(typeof str);
console.log(typeof undef);
console.log(typeof nully);

// 4. Ändere den Typ einer der Variablen (z.B. num) in einen anderen Typ (z.B. in einen String) und überprüfe den Typ erneut mit typeof.
// num = "42";
num = "42";
console.log(typeof num);

// 5. Speichere die Datei und führe sie in einem Browser oder in einer Konsole aus.

// 6. Stelle sicher, dass du die Ausgabe im Browser oder in der Konsole überprüfst, um zu sehen, ob du die Aufgabe richtig gelöst hast.
